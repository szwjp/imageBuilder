#### 固件地址 `https://192.168.1.1` (nginx 主入口, 自签证书; 备用 uhttpd `http://192.168.1.1:8080`)
#### 用户名 `root` 密码：无
#### 默认软件包大小 1GB

> 关于地址：多网口设备 LAN 默认为 `192.168.1.1`，可在触发构建时用 `custom_router_ip` 改成其他地址；
> **单网口设备为 DHCP 模式**，地址由上级路由分配，请从上级路由的客户端列表或设备屏幕确认。
